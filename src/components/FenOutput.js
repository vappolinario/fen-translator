import React from 'react';

const FenOutput = (props) => {
    return (
        <div>
            <h3>Translation</h3>
            <span>{props.value}</span>
        </div>
    );
};

export default FenOutput;

